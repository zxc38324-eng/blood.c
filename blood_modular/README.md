# blood.c v2.0 - Modular Cheat

Модульная версия blood.c с привязкой к конкретным играм и поддержкой хуков для обхода античита.

## 📁 Структура проекта

```
blood_modular/
├── loader.lua          # Главный загрузчик (запускать этот файл)
├── core/
│   ├── hooks.lua       # Хуки для обхода античита
│   ├── ui.lua          # UI система
│   └── utils.lua       # Общие функции
└── modules/
    ├── combat.lua      # Камлок, Хитбокс, Автошот, Fast Rev
    ├── movement.lua    # Флай, Спид, Телепорт
    ├── visuals.lua     # ESP, Crosshair, Drochka
    └── misc.lua        # Auto Reload, Auto Stomp, Anti AFK
```

## 🎮 Поддерживаемые игры

Скрипт работает **только** в следующих играх:

- **Da Hood** (PlaceId: 2788229376)
- **Da Hood VC** (PlaceId: 7213786345)
- **Hood Customs** (PlaceId: 9825515356)

### Как добавить свою игру

Откройте `loader.lua` и добавьте PlaceId вашей игры:

```lua
local ALLOWED_GAMES = {
    [2788229376] = "Da Hood",
    [YOUR_PLACE_ID] = "Your Game Name", -- Добавь сюда
}
```

Чтобы узнать PlaceId текущей игры, выполни в консоли:
```lua
print(game.PlaceId)
```

## 📥 Установка

### Вариант 1: Загрузка с GitHub (рекомендуется)

1. Загрузи все файлы на свой GitHub репозиторий
2. В `loader.lua` замени `YOUR_USERNAME` на свой GitHub username:
```lua
local BASE_URL = "https://raw.githubusercontent.com/YOUR_USERNAME/blood.c/main/blood_modular/"
```
3. Запусти через эксплойт:
```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/YOUR_USERNAME/blood.c/main/blood_modular/loader.lua"))()
```

### Вариант 2: Локальная загрузка

1. Сохрани все файлы в папку `workspace` твоего эксплойта
2. Запусти:
```lua
loadstring(readfile("blood_modular/loader.lua"))()
```

## 🎯 Функции

### 🎯 Combat (Legit & Star)
- **Camlock** - Автоматическое наведение камеры
  - Smoothness (1-100)
  - Prediction (0-0.5)
  - Hotkey bind
  - Auto-disable при убийстве
- **FOV Circle** - Круг для выбора цели
- **Hitbox Expander** - Увеличение хитбоксов (1-20)
- **Auto Shot** - Автоматический выстрел при выстреле хоста
- **Fast Rev** - Быстрая стрельба (25ms)

### 🏃 Movement
- **Walkspeed** - Увеличение скорости (16-350)
- **JumpPower** - Увеличение прыжка (50-350)
- **Infinity Jump** - Бесконечные прыжки
- **Fly** - Полет (10-200 скорость)
- **Noclip** - Прохождение сквозь стены

### 👁️ Visuals
- **ESP** - Boxes, Names, Distance, Health
- **Crosshair** - Кастомный прицел
- **Drochka** - Анимация (видна всем)

### 🔧 Misc
- **Auto Reload** - Автоматическая перезарядка
- **Auto Stomp** - Автоматический стомп K.O. игроков
- **Anti AFK** - Защита от кика за AFK

## 🛡️ Обход античита

Скрипт использует хуки для обхода античита:

- **Namecall Hook** - Блокирует античит ремоуты
- **Index Hook** - Спуфит значения WalkSpeed/JumpPower
- **NewIndex Hook** - Разрешает изменения от скрипта

### Настройка хуков

Открой `core/hooks.lua` и добавь свои ремоуты для блокировки:

```lua
if remoteName:find("anticheat") or 
   remoteName:find("kick") or
   remoteName:find("YOUR_REMOTE_NAME") then -- Добавь сюда
    return -- блокируем
end
```

## 🎮 Использование

### Загрузка
```lua
loadstring(game:HttpGet("URL_TO_loader.lua"))()
```

### Доступ к модулям из другого скрипта
```lua
local Blood = getgenv().BloodCheat

-- Получить настройки
print(Blood.Version)
print(Blood.Game)

-- Выгрузить скрипт
Blood.Destroy()
```

### Горячие клавиши
- **DELETE** - Выгрузить скрипт
- **Camlock Bind** - Настраивается в UI
- **Right Click на Toggle** - Забиндить на клавишу

## 🔧 Разработка

### Добавление нового модуля

1. Создай файл `modules/your_module.lua`:
```lua
local YourModule = {}

function YourModule.Init(ui)
    -- Твой код
end

function YourModule.Destroy()
    -- Очистка
end

return YourModule
```

2. Добавь в `loader.lua`:
```lua
Blood.Modules.YourModule = LoadModule("modules/your_module.lua")

-- В функции Init():
if Blood.Modules.YourModule then
    Blood.Modules.YourModule.Init(Blood.UI)
end
```

### Добавление новой функции в существующий модуль

Открой нужный модуль (например, `modules/combat.lua`) и добавь свою функцию:

```lua
local YourFeature = {
    Enabled = false,
    Setting1 = 10
}

local function UpdateYourFeature()
    -- Твой код
end

-- В функции Init() добавь UI:
local section = tabs.Legit:addSection({text = "Your Feature"})
section:addToggle({text = "Enable", state = false}):bindToEvent("onToggle", function(state)
    YourFeature.Enabled = state
    UpdateYourFeature()
end)
```

## ⚠️ Важно

1. **Замени `YOUR_USERNAME`** во всех файлах на свой GitHub username
2. **Добавь PlaceId** своей игры в `loader.lua`
3. **Настрой хуки** под свою игру в `core/hooks.lua`
4. **Не используй** в неподдерживаемых играх - скрипт не загрузится

## 📝 Changelog

### v2.0.0
- ✅ Модульная структура
- ✅ Привязка к конкретным играм
- ✅ Хуки для обхода античита
- ✅ Улучшенная организация кода
- ✅ Легкое добавление новых функций

## 🤝 Вклад

Хочешь добавить свой модуль? Создай Pull Request!

## 📜 Лицензия

MIT License - используй как хочешь

---

**Created by klermon**  
Profile: https://www.roblox.com/users/5271298342/profile
